to start the program:

load the extension onto firefox, log into postsql,
start fastapi with "python -m uvicorn backend.app:app --reload"

"C:\Program Files (x86)\Inno Setup 6\ISCC.exe" PuppeteerInno.iss

py -m PyInstaller --onefile --clean --name puppeteer-host --add-data "../.env;." native_messaging_host.py

py -m PyInstaller --clean puppeteer-backend.spec
